import { FindPositivePipe } from './find-positive.pipe';

describe('FindPositivePipe', () => {
  it('create an instance', () => {
    const pipe = new FindPositivePipe();
    expect(pipe).toBeTruthy();
  });
});
